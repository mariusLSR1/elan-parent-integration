# Parent site — copy to .env.local

# Élan deployment origin (no trailing slash). Used by next.config rewrites.
ELAN_ORIGIN=https://elan.referenceprof.fr

# Brand accent for Élan (postMessage elan:set-theme). NEXT_PUBLIC_ required for client components.
NEXT_PUBLIC_SITE_BRAND_ACCENT=#E8620A
